{
    "name": "@joamag/cleanup-hosted",
    "version": "0.4.0",
    "description": "Cleanups an hosted GitHub Runner environment",
    "repository": {
        "type": "git",
        "url": "git+https://gitlab.stage.hive.pt/joamag/cleanup-hosted.git"
    },
    "license": "Apache-2.0",
    "scripts": {
        "build": "ncc build index.js -o dist",
        "watch": "nodemon --ignore /dist/*.js --exec npm run build",
        "test": "node --test \"test/**/*.test.js\""
    },
    "dependencies": {
        "@actions/core": "^1.11.1"
    },
    "devDependencies": {
        "@vercel/ncc": "^0.38.4",
        "nodemon": "^3.1.14"
    }
}
